import Router from "./router";
import { FC } from "react";

import "../index.css";

type Props = {};

export const App: FC<Props> = ({}) => {
  return (
    <>
      <Router />
    </>
  );
};

export default App;
